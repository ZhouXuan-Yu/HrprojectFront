# 登录页动态设计方案

## 目标

当前登录页是静态白卡片，和招聘系统后续页面的企业工具气质不冲突，但缺少第一眼记忆点。改造目标是做成一个有鼠标响应的动态登录页：鼠标移动时背景的光晕、网格和节点轻微偏移，登录卡片保持清晰、稳定、可读。

## 使用的技能

- `frontend-design`：用于确定视觉锚点、内容纪律和整体页面气质。
- `css-animations`：用于设计可控的 CSS 动效，避免动画依赖不可预测的时间和布局变化。
- `design-trends`：用于提炼近期动态页面趋势，包括暗色主画布、鼠标互动、微动效、性能和可访问性约束。

## 外部参考

- GitHub `animated-login` topic：近期仍有 2025/2026 更新的 animated login 仓库，常见方向包括纯 CSS 动效、动态背景、平滑 hover、响应式表单。
  https://github.com/topics/animated-login?l=css
- GitHub `mouse-effect` topic：鼠标粒子、指针辉光、归一化鼠标坐标、React 鼠标粒子组件等鼠标响应模式。
  https://github.com/topics/mouse-effect
- `panwar8279/Mousemove-animation`：README 明确描述了元素随鼠标实时响应、速度控制、易集成等做法。
  https://github.com/panwar8279/Mousemove-animation
- `MohammedNainia/Eye-tracking-animation`：使用 `mousemove`、`translate` 和 hover 状态做眼球追踪与自定义光标互动。
  https://github.com/MohammedNainia/Eye-tracking-animation
- Particles login tutorial：总结了粒子背景、glassmorphism、neon glow、移动端降低粒子数量、`prefers-reduced-motion` 等登录页实践。
  https://77cod-ing.github.io/Project12.html
- particles.js 官方仓库：轻量粒子背景库，适合参考粒子交互模型；本项目不直接引入外部依赖。
  https://github.com/VincentGarreau/particles.js/

## 设计方向

视觉锚点选择 `Industrial`。原因：这是 HR 招聘工作台，不适合做成营销页式的大面积紫蓝渐变；工业化暗色界面更像内部系统的“任务入口”，同时能承载鼠标驱动的技术感。

关键视觉：

- 暗色全屏背景，主色为近黑和深蓝黑。
- 鼠标位置写入 CSS 变量 `--mx`、`--my`，驱动径向光晕、网格位移、节点偏移。
- 登录卡片使用半透明深色玻璃，但边界清晰，避免内容被背景抢戏。
- 主按钮使用招聘系统原有蓝色作为动作色，保证进入系统的操作识别。
- 角色选择保留三种演示角色，改成三枚稳定的分段卡片。

## 交互策略

- 鼠标移动只改变 transform、background-position 和 CSS 变量，不动画布局属性。
- 背景有轻微常驻漂移，鼠标进入后产生更明显的视差。
- 输入框、角色卡、登录按钮提供 hover/focus 状态。
- `prefers-reduced-motion: reduce` 下关闭常驻动画和鼠标视差，保留静态层次。
- 移动端不依赖鼠标，背景使用中心光晕，表单垂直铺排。

## 实现约束

- 不新增外部库，避免登录页为了视觉效果引入额外运行时。
- 保留现有登录逻辑：选择角色、写入 `localStorage`、跳转招聘看板。
- 保留 E2E 可定位文案：`HR 专员`、登录按钮、账号和密码。
- 不改变业务页面的全局样式，只改 `public/legacy/login.html`。
